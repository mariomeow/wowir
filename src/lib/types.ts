export type USER_TYPE = {
    id: string,
    username: string,
    avatar: string,
    isAdmin: boolean
}

export type RAID_TYPE = {
    id: number,
    name: string,
    maxAttendance: number,
    reference: string
}